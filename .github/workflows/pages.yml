name: Build and Deploy GitHub Pages 

on:
  push:
    branches: [ "web-druid" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm ci --prefix tools/sitegen || npm install --prefix tools/sitegen

      - name: Build site
        run: |
          npm run build --prefix tools/sitegen

      - name: Copy web-druid editor
        run: |
          mkdir -p site/programs/41_blackbird
          cp -r releases/41_blackbird/druid/src/web-druid site/programs/41_blackbird/web-druid

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
